#!/bin/bash

# right now I still like to execute everything from the share directory because there is no zsh autocomplete support
# if you want to have your output outside of the source tree change this environment variable
export CAFOUTPUTDIR="$CAFANALYSISBASE/share"

export CAFAUTOCOMPLETEOPT=3
